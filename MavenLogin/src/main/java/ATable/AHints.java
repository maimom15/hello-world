package ATable;

public class AHints {

}

/*

=======================================
Below tags are important to run springMVC annotation like @Controller, @RequestMapping

xmlns:mvc="http://www.springframework.org/schema/mvc" 

http://www.springframework.org/schema/mvc 
http://www.springframework.org/schema/mvc/spring-mvc.xsd">
 
<mvc:annotation-driven/>

===========================================

What is Gson. Gson is a Java library that can be used to convert Java Objects 
into their JSON representation. It can also be used to convert a JSON string to
an equivalent Java object.

==========================================

Either we can add the Jackson_Lib(json lib) folder inside librar's or SpringRegister2Library's
folder inside librar's  -- testing completed only SpringRegister2Library's library's are working.

==========================================
What is the use of hbm2ddl.auto in hibernate configuration file(<property name="hbm2ddl.auto">update</property>)

Automatically validates or exports schema DDL to the database when the
SessionFactory is created. With create-drop, the database schema will be
dropped when the SessionFactory is closed explicitly.

validate | update | create | create-drop
validate- existing schema
update- only update your schema once created
create- create schema every time


=============================================


*/
